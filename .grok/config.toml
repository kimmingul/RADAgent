# RADAgent 프로젝트 공유 설정.
# API 키, 토큰, 프로바이더 시크릿을 이 파일에 넣지 않는다.
#
# 프로젝트 .grok/config.toml 이 실제로 반영하는 구간은
# [mcp_servers], [plugins], [permission], [mcp] max_output_bytes 뿐이다.
# 그 밖은 사용자 설정(~/.grok/config.toml)에서만 읽힌다.
#
# 코딩 모델 힌트 (주석만, 설정 아님): 구현 세션은 grok-4.7.
# 모델 id를 이 파일에 적어도 프로젝트 설정은 모델을 선택하지 않는다.
